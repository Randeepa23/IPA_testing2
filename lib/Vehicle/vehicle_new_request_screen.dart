// vehicle_new_request_screen.dart
import 'package:flutter/material.dart';

class VehicleNewRequestScreen extends StatelessWidget {
  // final Map<String, dynamic> user;
  const VehicleNewRequestScreen({super.key, /* required this.user */});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("New Vehicle Request Screen"));
  }
}